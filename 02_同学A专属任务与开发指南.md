# 《同学 A 专属任务与开发指南》

> 项目：GKG 全球新闻语义分析与主题追踪平台
> 适用对象：**同学 A（全部前端 + QueryService + ExportService + F-13 PageRank + F-16 情感分析）**
> 版本：v3.0 / 2026-06-09
> 配套文档：`01_系统设计文档.md`、`03_同学B专属任务与开发指南.md`、`04_协作文档.md`

---

## 一、核心职责定位

**一句话**：你独立完成整套 Swing 桌面界面、Controller 事件协调、所有可视化图表（含 F-20 共现网络图自绘）、结果导出；在后端侧负责"贴近 UI 的查询/导出层"以及**两个有学习价值的挖掘算法（F-13 PageRank 焦点人物 + F-16 情感趋势与拐点检测）**——这两个算法分别与你的 F-20 网络图和 F-23 仪表盘**端到端垂直耦合**，从算法到画面整条链路都在你手里。

**核心收益**：
- 学到 **图挖掘**（PageRank 幂迭代算法 / 排名解释）
- 学到 **时序分析**（按日聚合 + 滑动窗口拐点检测）
- 答辩时你能讲清"为什么这个人是焦点节点"和"为什么这天情感反转"

---

## 二、模块与任务清单

### 模块 A1：QueryService（后端 / F-06、F-07、F-08、F-09、F-10）

> 依赖：B 提供的 `DbHelper` + Model + DAO（最晚 W2 末交付）。

- [ ] `service/impl/QueryServiceImpl.java`：
  - `search(QueryCondition, page, size)`：动态 SQL + `PreparedStatement` + `LIMIT/OFFSET`
  - `getPersonProfile(name)`：频次 + 平均 tone + 关联组织/人物 TOP10
  - `getOrgProfile(name)`：同上
  - `getThemeTrend(code, from, to)`：`GROUP BY publish_date` 聚合频次与平均 tone
  - `suggestPerson/suggestOrg(prefix, limit)`：`LIKE 'prefix%' LIMIT N`

### 模块 A2：ExportService（后端 / F-04）

- [ ] `service/ExportService.java` 接口（你定义）
- [ ] `service/impl/ExportServiceImpl.java`：
  - `exportCsv` → OpenCSV + UTF-8 BOM
  - `exportExcel` → Apache POI XSSFWorkbook
  - `exportJson` → Jackson 美化输出
- [ ] 反射工具：根据 record 的 `RecordComponent` 自动产出表头

### 模块 A3：F-13 PageRank 焦点人物算法（后端 / 算法）⭐ 学习重点

> 依赖：B 在 W4 末把 `cooccurrence` 表填好。你读这张表跑算法即可。

- [ ] `algorithm/PageRankRunner.java`
  - 输入：`cooccurrence` 表（边列表 + 权重 `co_count`）+ topN
  - 输出：`List<FocusNode(name, pageRank, degree)>`
  - **推荐自己实现幂迭代**（约 20 行，学习价值高于直接调 JUNG）：
    ```
    1. 把节点编号 0..N-1
    2. 初始化 pr[i] = 1/N
    3. 迭代 50 次：pr_new[i] = (1-d)/N + d * Σ_j (pr[j]*w_ji/outDeg[j])
       其中 d = 0.85（阻尼因子）
    4. 终止条件：max|pr_new - pr| < 1e-6
    5. 排序取 topN
    ```
  - 同时提供"基线版本"用 JUNG `PageRank` 类做对照验证（写在测试里）
- [ ] `src/test/.../PageRankRunnerTest.java`：4 节点小图固定结果断言；与 JUNG 版结果对比误差 < 1e-3

### 模块 A4：F-16 情感趋势与拐点检测算法（后端 / 算法）⭐ 学习重点

> 依赖：B 提供的 `GkgRecordDao` + `Person/Org/Theme` DAO 即可，不依赖其他算法。

- [ ] `algorithm/SentimentTrend.java`
  - 输入：`EntityRef`（人物/组织/主题）+ 日期范围
  - 输出：`SentimentSeries(series, inflectionPoints)`
  - 步骤：
    1. SQL 找出该实体相关的所有 `record_id` → JOIN `gkg_record` 取 `tone` + `publish_date`
    2. 按日 GROUP BY 算 `avg(tone)` → `List<DateTone>`
    3. **滑动窗口拐点检测**（窗口大小 W=3）：
       - 对每个时间点 i，比较 `avg(tone[i-W..i-1])` 与 `avg(tone[i+1..i+W])` 的符号
       - 若符号反转（正→负 或 负→正）且差值绝对值 > 阈值（如 1.5）→ 拐点
    4. 返回完整曲线 + 拐点列表
- [ ] `src/test/.../SentimentTrendTest.java`：人工构造 14 天数据 → 验证拐点位置

### 模块 A5：界面骨架与全局风格（前端 / W1~W2）

- [ ] `App.java`：`main()` 调 `DbHelper.init()`、设置 FlatLaf、显示 `MainFrame`
- [ ] `view/MainFrame.java`：`JFrame` + `JTabbedPane` 四 Tab（数据 / 查询 / 挖掘 / 关于）
- [ ] 顶部菜单栏（文件/视图/帮助）+ 底部状态栏（数据库记录数）
- [ ] 全局字体：`new Font("微软雅黑", Font.PLAIN, 13)`
- [ ] `common/UiUtil.java`、`common/GuiTask.java`：UI 工具与 `SwingWorker` 基类

### 模块 A6：数据管理面板（前端 / F-01、F-03、F-04）

- [ ] `view/ImportPanel.java`：文件/目录选择 + 进度条 + 状态标签 + 结果汇总
- [ ] 「数据清理」按钮（调 B 的 `cleanInvalidData()`）
- [ ] 「数据导出」按钮（调你自己的 ExportService）
- [ ] `controller/ImportController.java`：异步 `SwingWorker` 串接

### 模块 A7：查询检索面板（前端 / F-06、F-07、F-08、F-09、F-10）

- [ ] `view/QueryPanel.java`：内含三个子 Tab
  - 组合查询子页：条件区 + JTable 分页
  - 人物档案子页 + 自动补全
  - 组织档案子页 + 自动补全
- [ ] `view/ThemeTrackPanel.java`（含 TrendLineChart）
- [ ] `controller/QueryController.java`

### 模块 A8：挖掘分析面板（前端）

- [ ] `view/AnalysisPanel.java`：左侧操作 + 右侧结果
- [ ] 「构建/刷新共现网络」按钮 → 调 B 的 `rebuildCooccurrence()` （耗时操作，必须异步）
- [ ] 「PageRank 焦点 TOP-N」滑块 → **调你自己的 PageRank**（通过 `AnalysisService.topFocusPersons`，B 装配后会代理到你的算法类）
- [ ] 「主题热度」（粒度 + 范围）+ 表格 → 调 B 的 `themeHeatRank`
- [ ] 「主题聚类」（k 输入）→ 调 B 的 `clusterByKMeans`，结果用表格展示
- [ ] 「情感趋势」（实体类型 + 名称 + 范围）→ **调你自己的 SentimentTrend**（通过 `AnalysisService.sentimentTrend`）
- [ ] `controller/AnalysisController.java`

### 模块 A9：可视化组件（前端 / F-20、F-21、F-22、F-23）

> ❌ 砍掉选做 F-24 引语墙，把时间留给 F-20 网络图和 F-23 仪表盘的视觉打磨。

- [ ] `view/chart/CooccurNetworkPanel.java`（F-20）⭐ **重点**
  - 自绘 `JPanel`，力导向布局简化版（Fruchterman-Reingold 50 次迭代）
  - 节点直径 = 你的 PageRank 值映射
  - 边粗细 = 共现次数映射
  - 鼠标悬浮显示名称 / 滚轮缩放 / 左键拖拽节点
- [ ] `view/chart/TrendLineChart.java`（F-21）：JFreeChart `TimeSeriesChart`
- [ ] `view/chart/ThemeHeatmap.java`（F-22）：JFreeChart `XYBlockRenderer`
- [ ] `view/chart/SentimentDashboard.java`（F-23）⭐ **重点**
  - 左 DialPlot 显示总体 tone
  - 右自绘雷达图显示多维情感
  - **底部嵌入你的 SentimentTrend 拐点标注**（在折线上画红点 + 提示气泡）

### 模块 A10：打包与答辩材料

- [ ] 与 B 共同维护 `pom.xml` 的 `shade-plugin`，`mvn package` 出 fat-jar
- [ ] `run.bat` / `run.sh` 启动脚本
- [ ] 项目根 `README.md`（含截图）
- [ ] 答辩 PPT 制作 + 录屏

---

## 三、交付物清单

### 3.1 后端代码

| 路径 | 说明 |
|---|---|
| `service/impl/QueryServiceImpl.java` | F-06~F-10 实现 |
| `service/ExportService.java` + `impl/ExportServiceImpl.java` | F-04 |
| `algorithm/PageRankRunner.java` | F-13 PageRank ⭐ |
| `algorithm/SentimentTrend.java` | F-16 情感趋势 + 拐点 ⭐ |

### 3.2 前端代码

| 路径 | 说明 |
|---|---|
| `App.java` | 程序入口 |
| `view/MainFrame.java` | 主窗体 |
| `view/ImportPanel.java`、`QueryPanel.java`、`ThemeTrackPanel.java`、`AnalysisPanel.java` | 四个核心面板 |
| `view/chart/CooccurNetworkPanel.java` | F-20 网络图 ⭐ |
| `view/chart/TrendLineChart.java` | F-21 折线图 |
| `view/chart/ThemeHeatmap.java` | F-22 热力图 |
| `view/chart/SentimentDashboard.java` | F-23 仪表盘 ⭐ |
| `controller/ImportController.java`、`QueryController.java`、`AnalysisController.java` | 三个 Controller |
| `common/UiUtil.java`、`common/GuiTask.java` | 工具 |

### 3.3 测试

| 路径 | 说明 |
|---|---|
| `src/test/java/edu/gkg/algorithm/PageRankRunnerTest.java` | 4 节点小图 + 与 JUNG 对照 |
| `src/test/java/edu/gkg/algorithm/SentimentTrendTest.java` | 人工构造 14 天数据验证拐点 |

### 3.4 资源与文档

| 路径 | 说明 |
|---|---|
| `src/main/resources/icons/*.png` | 主界面图标 |
| `run.bat`、`run.sh` | 启动脚本 |
| `README.md` | 主 README |
| `screenshots/` | 答辩用截图（≥ 10 张） |
| 实验报告 第 4 章「查询与导出后端」 | A 撰写 |
| 实验报告 第 6 章「界面、可视化与 PageRank/情感算法」 | A 撰写 |

---

## 四、接口依赖与协作约定

### 4.1 我（A）需要为同学 B 提供的

| 交付物 | 截止 | 用途 |
|---|---|---|
| `algorithm/PageRankRunner.java`（含输入 `cooccurrenceDao` + 输出 `List<FocusNode>` 的稳定签名） | **W5 末** | B 在 `AnalysisServiceImpl.topFocusPersons` 中 `new PageRankRunner(...).topN(n)` 调用 |
| `algorithm/SentimentTrend.java`（输入 `EntityRef + 日期范围`，输出 `SentimentSeries`） | **W6 中** | B 在 `AnalysisServiceImpl.sentimentTrend` 中调用 |
| `QueryService` Impl | W3~W4 | B 在自测时可调（一般不调） |
| `ExportService` Impl | W3 | B 在 CLI 中可选用 |

### 4.2 我（A）需要依赖的

| 来自 B | 我何时需要 | 若 B 延迟我怎么办 |
|---|---|---|
| `pom.xml` + `schema.sql` + `DbHelper` + `AppConfig` | W1 末 | 提前催；最坏情况下我用自己的最小 pom 临时启动 |
| 所有 Model（含共享 record） | W1 末骨架，W2 末完整 | 用空 record 也能让 UI 编译 |
| 所有 DAO | W2 末 | 我写 QueryServiceImpl 时**直接依赖 DAO 真方法**，没 DAO 写不了；催 B 是关键 |
| `ImportServiceImpl` | W3 末 | 等期间用 `MockImportService`（伪进度条 + 固定 ImportResult） |
| `cooccurrence` 表填好（B 跑过 `rebuildCooccurrence` 真实数据） | **W4 末** | 我可以先用人工 SQL 往 `cooccurrence` 表插 5~10 条假边自测 PageRank |
| `AnalysisServiceImpl` 装配完成（调用我的算法） | W6 中 | 我可以在 UI 中**直接 new PageRankRunner / SentimentTrend** 临时跳过 Service 层 |
| `ThemeRanker` + `KMeansClusterer` 真实现 | W5 / W6 | 用 `MockAnalysisService` 返回假数据先调 UI |

### 4.3 Mock 方案

新建 `src/main/java/edu/gkg/mock/`，仅在 `-Dmock=true` 启用：

```java
// 等 B 完成 F-12 共现 + F-14 热度 + F-15 聚类期间用
public class MockAnalysisService implements AnalysisService {
    private final PageRankRunner realPageRank; // 我自己的真实现可以直接用
    private final SentimentTrend realSentiment; // 同上

    @Override public List<FocusNode> topFocusPersons(int topN) {
        // 用真 PageRank 跑假的 cooccurrence 数据
        return realPageRank.topN(topN);
    }
    @Override public List<ThemeHeat> themeHeatRank(Granularity g, LocalDate f, LocalDate t, int n) {
        // 假数据：返回 n 条 ThemeHeat
        return IntStream.range(0, n)
            .mapToObj(i -> new ThemeHeat("THEME-" + i, "2024-01", (int)(Math.random()*1000)))
            .toList();
    }
    @Override public List<Cluster> clusterByKMeans(int k, int maxIter) {
        return IntStream.range(0, k).mapToObj(i -> new Cluster(i,
            List.of("kw"+i+"-A","kw"+i+"-B","kw"+i+"-C"), (int)(Math.random()*200)+50)).toList();
    }
    @Override public SentimentSeries sentimentTrend(EntityRef ref, LocalDate f, LocalDate t) {
        return realSentiment.compute(ref, f, t); // 用我自己的真算法
    }
    @Override public void rebuildCooccurrence() { /* no-op */ }
}
```

### 4.4 协作规则

1. **Git 分支**：`feature/frontend-A`，包含所有 UI + QueryServiceImpl + ExportService + 你那两个算法
2. **每周日 22:00 前合并到 develop**，参与每周三晚联调
3. **算法类签名一旦交付不再改**（B 在装配 AnalysisService 时依赖它）
4. **共享目录红线**：你不能改 B 的 `model/`、`dao/`、`service/parser/`、`service/impl/ImportServiceImpl.java`、`service/impl/AnalysisServiceImpl.java`、`algorithm/{CooccurrenceBuilder,ThemeRanker,KMeansClusterer}.java`、`resources/schema.sql`；B 不能改你的 `view/`、`controller/`、`algorithm/{PageRankRunner,SentimentTrend}.java`、`service/impl/{QueryServiceImpl,ExportServiceImpl}.java`、`service/ExportService.java`、`App.java`
5. **提交规范**：`feat(ui)`、`feat(query)`、`feat(export)`、`feat(algo:pagerank)`、`feat(algo:sentiment)`、`fix(...)`、`test(...)`

---

## 附录 A：UI 截图清单（答辩必备 ≥ 10 张）

1. 主窗口首页 / 2. 数据导入进度 / 3. 综合查询结果表 / 4. 人物档案卡 / 5. **共现网络图（你的 PageRank 高亮节点）⭐** / 6. 主题趋势折线 / 7. 主题热力图 / 8. **情感仪表盘（你的拐点标注）⭐** / 9. 主题聚类结果 / 10. 数据导出对话框

## 附录 B：开发顺序速查（与 04_协作文档.md 严格对齐）

| 周 | 重点交付 | 关键依赖 |
|---|---|---|
| W1 | 等 B 启动包；写 `App.java`、`common/UiUtil.java`、`GuiTask.java` | 依赖 B 的 W1 末交付 |
| W2 | `MainFrame`、`ImportPanel` / `QueryPanel` 静态界面；开始 `QueryServiceImpl.search` + `ExportService(CSV)` | 依赖 B 的 DAO |
| W3 | `ImportPanel` 联调；`QueryServiceImpl` 全部完成；`ExportService(Excel/JSON)` | B 的 `ImportServiceImpl` 就绪 |
| W4 | `QueryPanel` 档案子页 + `ThemeTrackPanel + TrendLineChart`；开始 `CooccurNetworkPanel` 骨架；**开始 `PageRankRunner` 算法实现** | B 完成 F-12 共现表 |
| W5 | **`PageRankRunner` 完成 + 单测 + 交付给 B**；`CooccurNetworkPanel` 集成 PageRank；`ThemeHeatmap` | B 完成 F-14 |
| W6 | **`SentimentTrend` 完成 + 单测 + 交付给 B**；`SentimentDashboard`（含拐点标注） | B 完成 F-15 + AnalysisServiceImpl 装配 |
| W7 | 全功能联调；性能压测；`mvn package`；`run.bat`；README | — |
| W8 | 实验报告第 4、6 章；答辩 PPT；录屏；演示彩排 | — |
